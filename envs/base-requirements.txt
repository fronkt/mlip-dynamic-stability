# Backend-agnostic harness deps (installed into every per-model env).
# Torch is installed separately from the cu128 index BEFORE this file (see envs/README.md).
ase>=3.23
phonopy>=2.26
pymatgen>=2024.5
mp-api>=0.41
jarvis-tools>=2024.4
numpy>=1.26
pandas>=2.2
pyarrow>=16
pyyaml>=6
matplotlib>=3.8
seaborn>=0.13
